[General]

# v1.10.0 Native Theme Feature Parity needs no authentication or provider
# settings in SDDM theme configuration. Authentication configuration stays
# exclusively in the broker/PAM configuration.
#
# Native branding configuration remains reserved for v1.13.0.
