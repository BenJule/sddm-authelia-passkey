name: 'Automation: Assign roadmap metadata'
run-name: "Roadmap metadata for #${{ github.event.issue.number || github.event.pull_request.number }}"

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  assign:
    name: Assign project, maintainer and active milestone
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    if: github.event_name == 'issues' || github.event_name == 'pull_request_target'
    env:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1
        with:
          egress-policy: block
          allowed-endpoints: |
            api.github.com:443

      - name: Add to development project
        if: ${{ env.PROJECT_TOKEN != '' && vars.ROADMAP_PROJECT_URL != '' }}
        uses: actions/add-to-project@5afcf98fcd03f1c2f92c3c83f58ae24323cc57fd
        with:
          project-url: ${{ vars.ROADMAP_PROJECT_URL }}
          github-token: ${{ secrets.PROJECT_TOKEN }}

      - name: Project automation not configured
        if: ${{ env.PROJECT_TOKEN == '' || vars.ROADMAP_PROJECT_URL == '' }}
        run: echo 'Project auto-assignment skipped; configure PROJECT_TOKEN and ROADMAP_PROJECT_URL to enable it.'

      - name: Assign BenJule and nearest open milestone
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
        with:
          script: |
            const number = context.payload.pull_request
              ? context.payload.pull_request.number
              : context.payload.issue.number;

            await github.rest.issues.addAssignees({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: number,
              assignees: ['BenJule'],
            });

            const current = context.payload.pull_request
              ? context.payload.pull_request.milestone
              : context.payload.issue.milestone;

            if (!current) {
              const { data: milestones } = await github.rest.issues.listMilestones({
                owner: context.repo.owner,
                repo: context.repo.repo,
                state: 'open',
                sort: 'due_on',
                direction: 'asc',
                per_page: 100,
              });

              const candidates = milestones
                .filter(m => !m.due_on || new Date(m.due_on).getTime() >= Date.now())
                .sort((a, b) => {
                  if (!a.due_on && !b.due_on) return a.number - b.number;
                  if (!a.due_on) return 1;
                  if (!b.due_on) return -1;
                  return new Date(a.due_on) - new Date(b.due_on);
                });

              if (candidates.length > 0) {
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: number,
                  milestone: candidates[0].number,
                });
                core.info(`Set milestone: ${candidates[0].title}`);
              } else {
                core.info('No open milestone available; leaving milestone unset.');
              }
            }
